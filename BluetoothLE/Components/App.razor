<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="bootstrap/bootstrap.min.css" />
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="fontawesome.css" />
    <link rel="stylesheet" href="BluetoothLE.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />

    <style type="text/css">
        @@font-face {
	        font-family: "Font Awesome 5 Brands";
	        font-display: block;
	        font-weight: 400;
	        src: url(/fonts/free-fa-brands-400.woff2) format("woff2")
        }
        .fa-brands {
            font-family: "Font Awesome 5 Brands";
        }

        .fa-strava {
            --fa: "\f428"
        }

        @@font-face {
            font-family: "MyRubik";
            src: url("/fonts/RUBIK-BOLD.TTF") format("opentype");
        }
        @@font-face {
            font-family: "MySwiss";
            src: url("/fonts/SWIS721 BLKCN BT BLACK.TTF") format("opentype");
        }
        html {
            overflow: clip;
        }
        body {
            background-color: black;
            font-family: "MySwiss";
        }
        main {
            height: 100vh;
        }
        article {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100%;
            width: 100%;

            position: absolute;
            left: 0;
            top: 0;

            z-index: 2;
        }

        button {
            position: absolute;
            top: 0;
            left: 0;
            z-index: 10;
        }
        article > div {
            color: white;
            background-color: black;
            padding: 0.1em 1em;
            font-size: 72pt;
            /*font-family: "Eternal UI"; /* or Nimbus Sans D OT B, or Swis721 BlkCn BT */
            /*font-family: "Nimbus Sans D OT";*/
            font-family: "Swis721 BlkCn BT";
            font-family: "MySwiss";
            font-stretch: condensed;
            font-weight: 900;
            -webkit-text-stroke: 2px black;
            flex-wrap: nowrap;
            text-align: center;
            line-height: 100%;
        }

        div.sub-text
        {
            font-size: 36pt;
        }

        video {
            position: absolute;
            width: 100%;
            left: 0;
            top: 0;
        }

        .increase {
            background-color: green;
            background-color: rgb(0 200 0 / 75%) !important;
            color: white;
        }

        .decrease {
            background-color: red;
            background-color: rgb(255 0 0 / 75%) !important;
            color: white;
        }

        #workout-display
        {
            display: grid;
            grid-template-columns: 1fr repeat(3, 6em) 1fr;
            grid-template-rows: repeat(2, calc(32pt + 16px)) 108px;
            gap: 2px;

            position: absolute;
            left: 0;
            bottom: 0;

            z-index: 5;

            width: 100%;
            
            color: white;
            font-size: 32pt;

            text-align: right;

            /*font-family: "Manrope";*/
            font-family: "MyRubik";
            font-variant-numeric: tabular-nums;
        }

        .target-grid
        {
            display: grid;
            grid-template-columns: 1fr 1fr;
            grid-template-rows: subgrid;

            /*background-color: rgba(0 0 0 / 50%);*/
        }

        .target-grid > div
        {
            background-color: rgb(0 0 0 / 50%);
        }

        .target-value
        {
            grid-row: 1;
            grid-column: 1;

            padding-right: 16px;
        }

        .upcoming-value
        {
            grid-row: 1;
            grid-column: 2;

            padding-right: 0.5em !important;
        }

        .upcoming-value-percent
        {
            grid-row: 1;
            grid-column: 2;

            width: 0;
            left: 0;
        }

        .current-value
        {
            grid-row: 2;
            grid-column: 1;

            padding-right: 16px;
        }

        .current-icon
        {
            grid-row: 2;
            grid-column: 2;
            text-align: left;
            font-size: 90%;
            padding-top: 4px;
            padding-left: 16px;
        }

        #heart-rate
        {
            grid-column: 2;
            grid-row: 1 / span 2;
        }

        #power {
            grid-column: 3;
            grid-row: 1 / span 2;
        }

        #cadence {
            grid-column: 4;
            grid-row: 1 / span 2;
        }

        #graph-container {
            display: grid;
            grid-column: 1 / span 5;
            grid-row: 3;

            grid-template-columns: min-content auto 5ch;
            grid-template-columns: 10ch calc(100vw - 20ch - 16px) 10ch;
            grid-template-columns: min-content 1fr min-content;
            grid-template-rows: 1fr 1fr;

            font-size: 24pt;
            font-weight: bold;

            gap: 8px;
            padding-top: 8px;

            background-color: rgba(0 0 0 / 85%);
        }

        #graph {
            grid-column: 2 / span 2;
            grid-row: 1 / span 2;

            text-align: left;
        }

        #hr-graph {
            grid-column: 2 / span 2;
            grid-row: 1 / span 2;
            text-align: left;
            z-index: 5;
            stroke-width: 2px;
        }

        #power-graph {
            grid-column: 2 / span 2;
            grid-row: 1 / span 2;
            text-align: left;
            z-index: 10;
            stroke-width: 2px;
        }

        #elapsed-time {
            grid-column: 1;
            grid-row: 2;

            text-align: left;
            padding-left: 0.5em;
        }

        #track-remaining-time {
            grid-column: 3;
            grid-row: 1;

            padding-right: 0.5em;
            padding-left: 0.5em;
            /*background-color: white;*/
            color: black;
            /*margin-right: 8px;*/

            /*border-radius: 8px;*/
            text-align: right;
        }

        #track-remaining-time > div {
            background-color: white;
            display: inline-block;
            padding-right: 8px;
            padding-left: 8px;
            border-radius: 8px;
        }

        #total-remaining-time {
            grid-column: 3;
            grid-row: 2;

            padding-right: 0.5em;
            margin-right: 8px;
        }

        #whip
        {
            position: absolute;
            color: white;
            font-size: 36pt;
            font-family: "Swis721 BlkCn BT";
            text-align: center;
            width: 100%;
            top: 16px;
        }

        #whip > div {
            display: inline-block;
            background-color: rgb(255 0 0 / 85%);
            padding-left: 32px;
            padding-right: 32px;
            background-color: rgb(255 0 0 / 75%);
            border: 2px solid white;
        }

    </style>

    <HeadOutlet />
</head>

<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
    <script src="_content/Blazor.Bluetooth/JSInterop.js"></script>
</body>

</html>
